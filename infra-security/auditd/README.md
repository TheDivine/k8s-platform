# auditd Baseline

This directory contains public-safe auditd rule examples for Linux and Kubernetes nodes.

Review every rule before using it on production nodes. Start with a small rule set and monitor log volume before expanding coverage.

