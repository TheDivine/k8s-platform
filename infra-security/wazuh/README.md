# Wazuh Baseline

This directory contains public-safe Wazuh agent examples and local rule placeholders.

Do not commit:

- enrollment secrets
- manager credentials
- private manager addresses
- customer-specific rules

Use `agent.conf.example` as shape documentation only.

