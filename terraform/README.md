# Terraform

This directory is for provider-managed infrastructure.

Expected future scope:

- DNS
- object storage
- registries
- firewall resources
- IAM/service accounts
- external load balancer resources

Do not commit `.terraform/`, `*.tfstate`, real `*.tfvars`, provider credentials, or backend credentials.
