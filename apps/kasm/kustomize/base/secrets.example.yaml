# DO NOT APPLY THIS FILE AS-IS.
# It contains placeholder values only. Create real secrets through SOPS,
# SealedSecrets, ExternalSecrets, or manual kubectl secret creation.
apiVersion: v1
kind: Secret
metadata:
  name: kasm-secrets-example
  namespace: kasm
  labels:
    app.kubernetes.io/name: kasm
    app.kubernetes.io/component: secrets-example
type: Opaque
stringData:
  KASM_ADMIN_PASSWORD: "CHANGE_ME"
  POSTGRES_PASSWORD: "CHANGE_ME"
  REDIS_PASSWORD: "CHANGE_ME"

