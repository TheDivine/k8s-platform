# Ansible

This directory is for OS and Kubernetes node configuration.

Expected scope:

- Linux hardening
- SSH hardening
- firewall baseline
- auditd
- fail2ban
- ClamAV
- Maldet for web/upload/file nodes only
- Kubernetes node prerequisites

Do not commit real inventories, vault files, private SSH keys, or host-specific secrets.
