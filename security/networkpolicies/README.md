# NetworkPolicies

Place curated Kubernetes NetworkPolicy baselines here.

Policy examples should be namespace-scoped, labeled, and documented with expected traffic flows. Start with isolated examples before applying to live workloads.

Do not copy generated policies from live exports without sanitizing labels, namespaces, and intent.
