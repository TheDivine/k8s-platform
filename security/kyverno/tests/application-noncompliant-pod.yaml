# Synthetic negative fixture: application namespaces must remain enforced.
apiVersion: v1
kind: Pod
metadata:
  name: application-noncompliant-policy-fixture
  namespace: policy-test-application
spec:
  containers:
    - name: application
      image: busybox:1.36.1
      command: ["sh", "-c", "sleep 3600"]
      securityContext:
        privileged: true
