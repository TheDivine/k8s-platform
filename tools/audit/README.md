# Audit Tools

Place repository audit helpers here.

Examples:

- secret scan wrappers
- generated-file detectors
- nested repository checks
- public safety reports
- large file and backup dump checks

Tools in this directory should be safe to run locally without contacting external services unless explicitly documented.
